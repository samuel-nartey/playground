on:
  push:
    branches:
      - main

jobs:
   build:
      name: first job
      runs-on: ubuntu-latest

      steps:
        - name: checkout code
          uses: actions/checkout@v6

        - name: print a statement
          run:  echo "checkout successful"

   test:
      runs-on: ubuntu-latest

      steps:
        - name: test your code
          run:   this is the second job

   security-scan:
      runs-on: ubuntu-latest

      steps:
        - name: job 3
          run:  echo this is the third job

   deploy: 
      runs-on: ubuntu-latest

      steps:
        - name: job 4
          run: this is the fourth job
